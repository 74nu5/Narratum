@page "/wizard"
@rendermode InteractiveServer
@inject NavigationManager Navigation
@inject GenerationService GenService

<PageTitle>Créer une histoire - Narratum</PageTitle>

<FluentStack Orientation="Orientation.Vertical" VerticalGap="24">
    <FluentLabel Typo="Typography.H2">✨ Créer une nouvelle histoire</FluentLabel>
    
    @* Step indicator *@
    <FluentCard>
        <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8">
            @for (var i = 0; i < this.stepTitles.Length; i++)
            {
                var stepIndex = i;
                <FluentButton Appearance="@(stepIndex == this.currentStep ? Appearance.Accent : Appearance.Neutral)"
                              Disabled="@(stepIndex > this.currentStep)"
                              @onclick="() => this.currentStep = stepIndex">
                    @(stepIndex + 1). @this.stepTitles[stepIndex]
                </FluentButton>
            }
        </FluentStack>
    </FluentCard>
    
    @* Step content *@
    <div>
        @switch (this.currentStep)
        {
            case 0: @* Monde *@
                <WorldEditor @bind-WorldName="this.worldName" @bind-WorldDescription="this.worldDescription" />
                break;
                
            case 1: @* Genre *@
                <FluentCard>
                    <FluentStack Orientation="Orientation.Vertical" VerticalGap="16">
                        <FluentSelect TOption="string" Label="Genre" @bind-Value="this.genre">
                            <FluentOption TOption="string" Value="Fantasy">Fantasy</FluentOption>
                            <FluentOption TOption="string" Value="SciFi">Science-Fiction</FluentOption>
                            <FluentOption TOption="string" Value="Mystery">Mystère</FluentOption>
                            <FluentOption TOption="string" Value="Horror">Horreur</FluentOption>
                            <FluentOption TOption="string" Value="Historical">Historique</FluentOption>
                        </FluentSelect>
                        <FluentTextArea Label="Style narratif (optionnel)" @bind-Value="this.narrativeStyle" Rows="3" 
                                      Placeholder="Ex: Épique, sombre, humoristique..." />
                    </FluentStack>
                </FluentCard>
                break;
                
            case 2: @* Personnages *@
                <CharactersEditor @bind-Characters="this.characters" />
                break;
                
            case 3: @* Lieux *@
                <LocationsEditor @bind-Locations="this.locations" />
                break;
                
            case 4: @* Résumé *@
                <FluentCard>
                    <FluentStack Orientation="Orientation.Vertical" VerticalGap="16">
                        <FluentLabel Typo="Typography.H4">📋 Récapitulatif</FluentLabel>
                        <FluentLabel><strong>Monde :</strong> @(this.worldName)
                        </FluentLabel>
                        <FluentLabel><strong>Description :</strong> @(this.worldDescription ?? "Aucune")
                        </FluentLabel>
                        <FluentLabel><strong>Genre :</strong> @(this.genre)
                        </FluentLabel>
                        <FluentLabel><strong>Style :</strong> @(this.narrativeStyle ?? "Par défaut")
                        </FluentLabel>
                        <FluentLabel><strong>Personnages :</strong> @this.characters.Count</FluentLabel>
                        <FluentLabel><strong>Lieux :</strong> @this.locations.Count</FluentLabel>
                        
                        @if (this.characters.Count > 0)
                        {
                            <FluentLabel Typo="Typography.Body"><strong>Liste des personnages :</strong></FluentLabel>
                            <ul>
                                @foreach (var c in this.characters)
                                {
                                    <li>@c.Name @(string.IsNullOrEmpty(c.Description) ? "" : $"- {c.Description}")</li>
                                }
                            </ul>
                        }
                    </FluentStack>
                </FluentCard>
                break;
        }
    </div>
    
    @* Navigation buttons *@
    <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="12" HorizontalAlignment="HorizontalAlignment.End">
        <FluentButton Appearance="Appearance.Neutral" @onclick='() => this.Navigation.NavigateTo("/")'> Annuler</FluentButton>
        
        @if (this.currentStep > 0)
        {
            <FluentButton Appearance="Appearance.Neutral" @onclick="this.PreviousStep">← Précédent</FluentButton>
        }
        
        @if (this.currentStep < this.stepTitles.Length - 1)
        {
            <FluentButton Appearance="Appearance.Accent" @onclick="this.NextStep" Disabled="@(!this.CanProceed)">
                Suivant →
            </FluentButton>
        }
        else
        {
            <FluentButton Appearance="Appearance.Accent" @onclick="this.CreateStory" Disabled="@(!this.CanCreate)">
                🚀 Créer l'histoire
            </FluentButton>
        }
    </FluentStack>
</FluentStack>


